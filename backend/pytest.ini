[pytest]
# 测试配置
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 异步测试配置
asyncio_mode = auto

# 输出配置
addopts = -v --tb=short

# 标记
markers =
    unit: 单元测试
    integration: 集成测试
    slow: 慢速测试
